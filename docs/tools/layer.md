# layer

Use `layer` to change the layer of the generated line while a template is
rendering.

## Available Functions

### `layer.set(value)`

Set the layer of the generated line.

```ass
Comment: 0,0:00:00.00,0:00:00.00,Default,,0,0,0,template syl,!layer.set(2)!{\pos($syl_center,$syl_middle)}
```
